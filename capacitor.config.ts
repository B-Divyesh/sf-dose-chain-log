import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'in.sociobot.dosechainlog',
  appName: 'Dose Chain Log',
  webDir: 'dist',
  backgroundColor: '#080B13',
  android: { backgroundColor: '#080B13' },
}

export default config
