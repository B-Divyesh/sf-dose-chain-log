# Dose Chain Log

Live: https://dose-chain-log.sociobot.in — built by the Param Factory (`android`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
